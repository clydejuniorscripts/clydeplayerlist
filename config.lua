Config = {}

-- Discord Webhook URL
-- Enter your Discord Webhook URL here if you want to enable logging to Discord.
Config.DiscordWebhookURL = ''

-- Text settings
Config.TextScale = 0.35   -- The scale of the ID text.
Config.TextFont = 4       -- The font of the ID text.
Config.TextProportional = 1 -- Whether the text is proportional.

-- Background settings
Config.BackgroundColor = {0, 0, 0, 150}  -- The background color behind the ID text in RGBA format.
Config.TextColor = {255, 255, 255, 255}  -- The color of the ID text in RGBA format.

-- Key mapping
Config.KeyMapping = {
    ShowPlayerList = "U"  -- The key used to show the player IDs. Default is "U".
}
